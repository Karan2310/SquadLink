import React, { useEffect } from "react";

const Home = () => {
  return (
    <div>
      {/* <div>Home</div>
      <h1>Home {user && user.name}</h1>
      <button radius="md" onClick={handleLogout}>
        Logout
      </button> */}
      hello
    </div>
  );
};

export default Home;
